import QtQuick 2.15
import QtQuick.Controls 2.15
import Mtoc.Backend 1.0

MenuSeparator {
    id: separator
    
    contentItem: Rectangle {
        implicitHeight: 1
        color: Theme.edgeLineColor
    }
}